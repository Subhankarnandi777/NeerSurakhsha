version: "3.9"
services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    depends_on:
      - db
  db:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_DB: neersurakhsha
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
